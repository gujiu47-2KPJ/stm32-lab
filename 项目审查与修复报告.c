/* =========================================================================
 * 项目审查与修复报告 (Project Review & Fix Report)
 *
 * 生成时间: 2025
 * 审查范围: 工作区 (g:\stm32project) 下所有 STM32 项目
 *
 * 阅读说明:
 *   每个项目下面列出:
 *     [问题描述] - 原代码里存在的bug/隐患
 *     [修改方案] - 修复的原理与做法
 *     [涉及文件] - 原文件名 / 修复后新文件名
 *
 *   所有"修改后"版本都以 _MODIFIED.c 或 _OPTIMIZED.c 结尾,
 *   与原文件放在同一目录, 便于"原文件对照学习 + 用新文件烧录"
 * =========================================================================
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ 项目 1: 智能风口系统 (标准库, Smart Vent)                          │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 原文件: 智能风口系统\user\main.c
 * 新文件: 智能风口系统\user\main_MODIFIED.c
 *
 * ┌── Bug1: TIM2 被重复初始化, 舵机完全失效
 * │ 现象: 舵机上电后不响应角度设置
 * │ 根因: PWM_Motor_Init() 内部调用 TIM_DeInit(TIM2) + TIM_Init(TIM2),
 * │       这会把之前舵机TIM2_CH1的PWM配置彻底覆盖
 * │ 修复: 三路PWM独立分配
 * │       TIM2 → 舵机 (PA0, 50Hz)
 * │       TIM3 → LED呼吸灯 (PA6, 100Hz)
 * │       TIM4 → 电机PWM (PB6, 1kHz)
 * │ 原理: 每个定时器只服务一个PWM用途, 互不干扰
 *
 * ┌── Bug2: PA2 引脚冲突, 电机方向与PWM共用同一IO
 * │ 现象: 电机乱转, 或 PWM 没输出
 * │ 根因: PA2 同时被定义为"电机方向AIN2"(推挽输出) 和
 * │       "TIM2_CH3 PWM"(复用推挽), 两个GPIO_Init互相覆盖
 * │ 修复: AIN2 从 PA2 移到 PB0 (纯推挽GPIO), PWM 用 TIM4_CH1 @ PB6
 * │ 原理: STM32每个GPIO只能有一种功能, 推挽和复用推挽不能同时激活
 *
 * ┌── Bug3: SysTick_Handler 被定义两次 → 链接失败
 * │ 现象: Build 时报 "multiple definition of `SysTick_Handler'"
 * │ 根因: stm32f10x_it.c 里有空的 SysTick_Handler, main.c 又写了一个
 * │ 修复: 注释掉 stm32f10x_it.c 中的版本, 只保留 main.c 的
 * │       (同理 USART1_IRQHandler 也只能留一个)
 *
 * ┌── Bug4: 锁机10秒 = 实际 10ms, 锁机几乎瞬间解除
 * │ 现象: 触发"锁机"后, 立即自己解除, 或者1~2秒就解除
 * │ 根因: 用 TIM3 更新事件(1kHz)自减 lock_timer,
 * │       每次中断减1, 减10次只用 10ms
 * │ 修复: 改用 SysTick 记录"解锁时刻"
 * │       lock_end_time = system_tick + 10000;   // 10秒后
 * │       每次 Update_System_Status 里判断 system_tick >= lock_end_time
 * │ 原理: SysTick 是 ARM Cortex-M 内置的系统滴答, 精度 1ms,
 * │       不会和任何外设冲突; "记录结束时刻" 比"计数器自减"更可靠,
 * │       因为即使处理被延迟, 下一轮依然能"到点就解除"
 *
 * ┌── Bug5: main 用 __WFI() 进入睡眠, 状态更新永远停
 * │ 现象: 上电后LED呼吸灯不亮, 锁机也不动, 看起来像死机
 * │ 根因: __WFI() 让 CPU 停止取指, 只有中断能唤醒,
 * │       放在 while(1) 里意味着"每循环就睡觉",
 * │       轮询式的 LED_Breathe/Update_System_Status 永远不会运行
 * │ 修复: 替换成普通的 while(1) { 轮询函数 }
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ 项目 2: vscode_stm32 (HAL库, 编码器+LED+串口)                      │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 原文件: vscode_stm32\Core\Src\main.c
 * 新文件: vscode_stm32\Core\Src\main_MODIFIED.c
 *
 * ┌── Bug1: 中文文本混入代码 → 编译直接失败
 * │ 现象: arm-none-eabi-gcc 报 "expected ';' before '帮'"
 * │ 根因: 第346行附近 "key_press = 0;帮我检查当前代码..."
 * │       注释符号(//)漏了, 导致"帮我检查..."被当作C代码
 * │ 修复: 把这段文本移到文件顶部的注释里, 或加上 // 前缀
 * │ 原理: C编译器把任意非关键字、非标识符、非运算符的文本
 * │       一律当成语法错误; 所以代码里不能"裸写"中文
 *
 * ┌── 优化点: EXTI 优先级 / NVIC 配置
 * │ TIM2溢出中断优先级 = 0, EXTI9_5 = 0
 * │ 如果编码器按键和 TIM2定时上报同时触发, 会排队
 * │ 方案: 把 EXTI9_5 优先级设为 1 (低于 TIM2, 避免抢占PWM中断)
 * │       或者保持现状 (当前配置没致命问题)
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ 项目 3: 蓝牙循迹小车 (标准库, Bluetooth Line-Follow Car)          │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 原文件: 蓝牙循迹小车\user\main.c
 * 新文件: 蓝牙循迹小车\user\main_MODIFIED.c  ← 参考 智能小车 的写法
 *
 * ┌── Bug1: while(1) 里所有循迹逻辑都被注释掉了, 实际不循迹
 * │ 现象: 小车原地不动, 只有蓝牙控制时才动
 * │ 根因: while (1) { // ...所有逻辑都是注释... }
 * │ 修复: 取消注释, 或用"加权位置法"重写(见 智能小车\main_OPTIMIZED.c)
 *
 * ┌── Bug2: 左大括号不匹配 (编译报错)
 * │ 原代码 USART1_IRQHandler 之前多了一个 }
 * │ 修复: 删除多余的右大括号, 或重写结构
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ 项目 4: FlashSystemDataProtect (HAL库, Flash掉电保护)             │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 原文件: FlashSystemDataProtect\Core\Src\main.c
 * 新文件: FlashSystemDataProtect\Core\Src\main_OPTIMIZED.c
 *
 * ┌── 优化点 1: 结构体对齐 (__packed 是 Keil 专有语法)
 * │ GCC下应使用 __attribute__((packed)) 或者 直接用 uint8_t 数组
 * │ 本项目用 uint8_t buf + 手动偏移 是最可靠的方案
 *
 * ┌── 优化点 2: 增加 MAGIC 字
 * │ 新板子上电时 Flash 是全 0xFF, 不加 MAGIC 会读成 "有数据"
 * │ 约定 STORAGE_MAGIC = 0xA55A 表示"数据有效"
 *
 * ┌── 优化点 3: 按键消抖从 HAL_Delay(20) 改为状态机
 * │ 非阻塞, 主循环每轮都能跑
 *
 * ┌── 优化点 4: OLED 按需刷新
 * │ 计数没变化就不重绘, 省时间 + 延长OLED寿命
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ 项目 5: 智能小车 (标准库, 5路红外)                                │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 原文件: 智能小车\user\main.c
 * 新文件: 智能小车\user\main_OPTIMIZED.c
 *
 * 主要优化: "15条if 判断每一种组合" → 改成 "加权位置法"
 *
 *   s1 s2 s3 s4 s5   → 权重 -2 -1 0 +1 +2
 *
 *   sum = -2*s1 -1*s2 +0*s3 +1*s4 +2*s5
 *
 *   sum < 0 → 左转 (越负转得越急)
 *   sum = 0 → 直行 (或脱线)
 *   sum > 0 → 右转
 *
 *   any = s1|s2|s3|s4|s5
 *   any = 0 → 脱线, 用上一次sum决定往哪边找
 *
 * 好处:
 *   - 代码从 100+行 缩到 30行 左右
 *   - 抖动小 (相邻两路同时触发不会来回猛切换)
 *   - 权重和阈值可继续调, 调参方便
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ 项目 6: BuleUSCar (HAL库, 蓝牙避障小车)                           │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 原文件: BuleUSCar\Core\Src\main.c
 * 新文件: BuleUSCar\Core\Src\main_MODIFIED.c
 *
 * ┌── Bug1: HAL_Init() 和 SystemClock_Config() 被调用两次
 * │ 第一次 CubeMX 自动生成, 第二次 用户代码里又写了一遍
 * │ 第二次 HAL_Init() 会把前一次初始化好的 SysTick 等推翻重来
 * │ 第二次 SystemClock_Config() 尝试使能已启的PLL → HAL_ERROR
 * │ 修复: 删除用户代码区里的重复调用, 只保留 CubeMX 生成的
 *
 * ┌── Bug2: 电机函数不对称 (左轮有前进/后退/停止, 右轮只有前进)
 * │ 修复: Motor_Right_Stop() / Motor_Right_Backward() 补齐
 * │ 或直接用 GPIO 电平控制 (AIN1/AIN2 四个引脚独立写)
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ 项目 7: roundoled (OLED显示项目)                                  │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 主要问题: main.c 里 main() 函数被定义了两次甚至三次
 * 现象: 编译报 "multiple definition of `main'"
 * 修复: 只留一套 main() / 初始化 / 显示逻辑
 *
 * 建议做法:
 *   - 用 CubeMX 重新 "Generate Code" 一次
 *   - 在 USER CODE BEGIN 2 之后只放自己的显示循环
 *   - 其余地方全部删除
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │ STM32 新手常见陷阱总结 (Top Traps)                                 │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 1. 定时器不能复用:
 *    同一个 TIMx 不能同时做 "50Hz舵机" 和 "1kHz电机",
 *    因为它们共用同一个 ARR(周期) 和 PSC(分频),
 *    频率只能是一种 → 每个用途要分配独立TIM
 *
 * 2. GPIO功能只能选一个:
 *    GPIOA Pin2 不能既是"普通推挽输出"(电机方向)又同时是
 *    "TIM2_CH3 PWM输出"(复用推挽) → 要用两个GPIO
 *
 * 3. 中断函数只能定义一次:
 *    stm32f10x_it.c 里的"空骨架" 与 main.c 里自己写的"实现",
 *    链接器会报 multiple definition → 必须注释掉其中一个
 *
 * 4. __WFI() 不是"省电的while(1)":
 *    用 __WFI() 后, 主循环里的轮询逻辑(LED呼吸/状态机)完全不跑,
 *    只适合"所有逻辑都在中断里"的极简应用
 *
 * 5. 时间基准:
 *    做"持续XX秒"这类需求, 应该"记录结束时刻", 而不是"循环自减",
 *    后者依赖"调用频率", 一旦主循环被阻塞(哪怕几ms), 计时就不准
 *
 * 6. 中文注释:
 *    用 // 或 /* ... *\/ 包住, 不要裸写在代码行里
 *    编译器看不懂中文, 只会当作语法错误
 *
 * 7. Flash写前必须擦:
 *    W25Q64 只能把 1 写成 0, 必须先整块擦掉才能写新数据,
 *    跳过"擦除"直接写会读到莫名其妙的数
 *
 * 8. OLED 刷新要省着来:
 *    I2C 刷新整屏需要 10~20ms,
 *    每轮都刷 = 按键响应会被拖慢 10ms,
 *    → "数值变了才重绘" 是最佳实践
 *
 * 9. 按键要消抖:
 *    物理按键按下/松开的瞬间会抖动 10~20ms,
 *    不处理会把"按一次"识别成"按了几次"
 *    HAL_Delay(20) 是最省事的做法, 状态机是非阻塞的升级版
 *
 * 10. 编码器接口要用硬件 TIM:
 *     TIM3/4 的硬件编码器接口(Encoder Mode)比软件轮询PA6/PA7
 *     快几个数量级, 而且能精确到 "每脉冲±1步"
 *     不要自己写软件判 A/B 相 → 又慢又容易错
 *
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * 如何替换 (How to use the fixed files):
 *
 *   1) 打开 Keil / STM32CubeIDE, 定位到要改的 main.c
 *   2) 把原 main.c 的内容全部复制备份 (或直接看 git diff)
 *   3) 用 _MODIFIED.c 或 _OPTIMIZED.c 的内容替换
 *   4) 根据你自己的硬件(引脚/PWM频率/串口波特率), 调整注释里
 *      标出的 "请根据你实际板子修改" 的部分
 *   5) 先 Build, 确认无错误后再烧录
 *   6) 若烧录后行为不对, 回头看"问题描述"部分里的分析,
 *      很可能是你的具体硬件引脚和默认假设不一致
 *
 * 重点提示 (对学习者最有用的一句话):
 *   ┌────────────────────────────────────────────────────┐
 *   │ 每个 bug 的真正价值不是"改好它",                      │
 *   │ 而是"理解它为什么会发生", 下次不写同样的错误.          │
 *   │                                                     │
 *   │ 本报告里每个 bug 都写出了"原因"和"底层原理",           │
 *   │ 看懂这些原因, 比直接复制粘贴新文件, 对你更有帮助.      │
 *   └────────────────────────────────────────────────────┘
 *
 * =========================================================================
 */