# 项目总结：FlashSystemDataProtect

## 接线图

| STM32 引脚 | 连接外设 |
|------------|----------|
| PA0  | 按键（清零键，上拉输入→GND） |
| PA1  | 按键（计数键，上拉输入→GND） |
| PA4  | W25Q64 CS（片选） |
| PA5  | W25Q64 SCK（SPI1时钟） |
| PA6  | W25Q64 MISO（DO数据输出） |
| PA7  | W25Q64 MOSI（DI数据输入） |
| PB6  | OLED SCL（硬件I2C1） |
| PB7  | OLED SDA（硬件I2C1） |
| PC13 | 板载LED |
| PA13 | ST-Link SWDIO |
| PA14 | ST-Link SWCLK |
| PD0  | 8MHz晶振 OSC_IN |
| PD1  | 8MHz晶振 OSC_OUT |
| 3.3V | W25Q64 VCC / OLED VCC |
| GND  | 公共地 |

## 项目效果

断电保护按钮计数器 — 按按钮计数并保存到 W25Q64 Flash，断电后数据不丢失。

## 功能描述

- PA1 计数键：按下计数+1，自动保存到 W25Q64 Flash
- PA0 清零键：按下归零，自动保存到 Flash
- SSD1306 OLED（I2C）实时显示当前计数值和上次断电时的值
- 每次上电自动从 Flash 恢复上次断电前的计数

## 完整度

✅ **项目完整，可以跑通**

## 库类型

**HAL 库**（STM32CubeMX 生成）

## 数据格式

```
Flash地址0x000000: [0xA5] [count:4字节] [checksum:1字节]  （__packed 紧凑结构体）
```

## 注意

需要外接 W25Q64 模块。

## 最后修改日期

2026-06-16（本项目有详细开发日志 `DEVELOPMENT_LOG.md`）
